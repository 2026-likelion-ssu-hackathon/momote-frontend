# Base URL of the team's backend (see src/api.js) — the 3 AI suggestion endpoints
# (/api/tone-correction, /api/date-course, /api/video-recommendation) are expected to live there.
# Copy this file to .env.local and point it at wherever that backend actually runs.
VITE_API_BASE_URL=http://localhost:4000
